import { extendTheme } from '@chakra-ui/react'

const tabsTheme = extendTheme({
    colors: {
        brand: {
            500: "#EAECF0",
            600: "#164C96",
        }
    }
});

export default tabsTheme;