import { extendTheme } from '@chakra-ui/react'

const tagsTheme = extendTheme({
    colors: {
        brand: {
            400: "#A9CBEB",
            500: "#F2F2F2",
        }
    }
});

export default tagsTheme;